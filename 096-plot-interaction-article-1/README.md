Simply displays the x and y coordinates of the mouse click
while it's inside the plot area.

Out side the plot area, no effect.

The coordinate values reflect the ranges of x-axis and y-axis variables
